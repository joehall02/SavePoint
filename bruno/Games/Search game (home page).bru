meta {
  name: Search game (home page)
  type: http
  seq: 7
}

post {
  url: {{base_url}}/{{game_route}}/search/
  body: json
  auth: inherit
}

body:json {
  {
    "searchParam": "Unauthorized"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
