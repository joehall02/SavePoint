meta {
  name: Edit game
  type: http
  seq: 4
}

put {
  url: {{base_url}}/{{game_route}}/2
  body: json
  auth: inherit
}

body:json {
  {
    "title": "blah blah test"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
