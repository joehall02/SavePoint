meta {
  name: Edit game
  type: http
  seq: 4
}

put {
  url: {{base_url}}/{{game_route}}/2
  body: json
  auth: inherit
}

body:json {
  {
    "condition": "Okay"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
